version: "3.8"

services:
  web:
    build: ./app
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./app:/app
    ports:
      - "8000:8000"
    env_file:
      - django.env
      - postgres.env
    depends_on:
      - db
  db:
    image: "postgres:13"
    volumes:
      - ./postgres:/var/lib/postgresql/data
    env_file:
      - postgres.env